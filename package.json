{
  "name": "polacode-automated",
  "displayName": "Polacode automated",
  "description": "📸 Beautiful screenshots for your code",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/silentnoodlemaster/polacode-automated.git"
  },
  "publisher": "silentnoodlemaster",
  "keywords": [
    "polacode",
    "polaroid",
    "screenshot",
    "snippet",
    "share"
  ],
  "galleryBanner": {
    "color": "#fbfbfb",
    "theme": "light"
  },
  "icon": "icon.png",
  "categories": [
    "Other"
  ],
  "engines": {
    "vscode": "^1.45.1"
  },
  "activationEvents": [
    "onCommand:polacode.activate",
    "onWebviewPanel:polacode"
  ],
  "main": "./src/extension",
  "contributes": {
    "commands": [
      {
        "command": "polacode.activate",
        "title": "📸 Polacode automated"
      }
    ],
    "configuration": {
      "title": "Polacode automated",
      "properties": {
        "polacode.background": {
          "type": "string",
          "markdownDescription": "Background color of the snippet's container. Can be any valid value of the CSS property `background`.",
          "default": "transparent"
        },
        "polacode.shadow": {
          "type": "string",
          "markdownDescription": "Shadow of the snippet node. Can be any valid value of the CSS property `box-shadow`. Ignored if `#polacode.target#` is `snippet`.",
          "default": "0 0 8px rgba(0,0,0,.18), 0 8px 16px rgba(0,0,0,.36)"
        },
        "polacode.padding": {
          "type": "number",
          "markdownDescription": "amount of space reserved around the snippet. Ignored if `#polacode.target#` is `snippet`.",
          "default": 32,
          "minimum": 0
        },
        "polacode.target": {
          "type": "string",
          "description": "Shoot with or without container.",
          "default": "container",
          "enum": [
            "container",
            "snippet"
          ],
          "markdownEnumDescriptions": [
            "Shoot the snippet with padding for `#polacode.shadow#`.",
            "Shoot the snippet on it's own."
          ]
        },
        "polacode.captureScale": {
          "type": "number",
          "markdownDescription": "Determines the final size of the capture. A scale of `1` equates to the apparent size of the capture preview.",
          "default": 2,
          "minimum": 1
        },
        "polacode.defaultPath": {
          "type": [
            "string",
            "null"
          ],
          "description": "Default save location for snippets.",
          "default": null
        },
        "polacode.windowTitle": {
          "type": "boolean",
          "description": "Display a title at the top of the snippet. Can be overridden by clicking the top of the snippet preview.",
          "default": false
        },
        "polacode.autoSave": {
          "type": "boolean",
          "description": "Automatically save snippet instead of displaying save dialog.",
          "default": false
        },
        "polacode.closeOnSave": {
          "type": "boolean",
          "description": "Close panel after saving a snippet.",
          "default": false
        }
      }
    }
  },
  "devDependencies": {
    "@fuelrats/eslint-config": "^2.2.0-canary.1",
    "@types/node": "^14.0.6",
    "@types/vscode": "^1.45.1",
    "eslint": "^7.1.0",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-jsdoc": "^26.0.1"
  },
  "dependencies": {
    "dom-to-image-even-more": "^1.0.4",
    "vivus": "^0.4.5"
  }
}
